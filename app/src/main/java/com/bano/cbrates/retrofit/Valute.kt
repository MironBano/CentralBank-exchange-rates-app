package com.bano.cbrates.retrofit

import com.bano.cbrates.retrofit.valutes.AED
import com.bano.cbrates.retrofit.valutes.AMD
import com.bano.cbrates.retrofit.valutes.AUD
import com.bano.cbrates.retrofit.valutes.AZN
import com.bano.cbrates.retrofit.valutes.BGN
import com.bano.cbrates.retrofit.valutes.BRL
import com.bano.cbrates.retrofit.valutes.BYN
import com.bano.cbrates.retrofit.valutes.CAD
import com.bano.cbrates.retrofit.valutes.CHF
import com.bano.cbrates.retrofit.valutes.CNY
import com.bano.cbrates.retrofit.valutes.CZK
import com.bano.cbrates.retrofit.valutes.DKK
import com.bano.cbrates.retrofit.valutes.EGP
import com.bano.cbrates.retrofit.valutes.EUR
import com.bano.cbrates.retrofit.valutes.GBP
import com.bano.cbrates.retrofit.valutes.GEL
import com.bano.cbrates.retrofit.valutes.HKD
import com.bano.cbrates.retrofit.valutes.HUF
import com.bano.cbrates.retrofit.valutes.IDR
import com.bano.cbrates.retrofit.valutes.INR
import com.bano.cbrates.retrofit.valutes.JPY
import com.bano.cbrates.retrofit.valutes.KGS
import com.bano.cbrates.retrofit.valutes.KRW
import com.bano.cbrates.retrofit.valutes.KZT
import com.bano.cbrates.retrofit.valutes.MDL
import com.bano.cbrates.retrofit.valutes.NOK
import com.bano.cbrates.retrofit.valutes.NZD
import com.bano.cbrates.retrofit.valutes.PLN
import com.bano.cbrates.retrofit.valutes.QAR
import com.bano.cbrates.retrofit.valutes.RON
import com.bano.cbrates.retrofit.valutes.RSD
import com.bano.cbrates.retrofit.valutes.SEK
import com.bano.cbrates.retrofit.valutes.SGD
import com.bano.cbrates.retrofit.valutes.THB
import com.bano.cbrates.retrofit.valutes.TJS
import com.bano.cbrates.retrofit.valutes.TMT
import com.bano.cbrates.retrofit.valutes.TRY
import com.bano.cbrates.retrofit.valutes.UAH
import com.bano.cbrates.retrofit.valutes.USD
import com.bano.cbrates.retrofit.valutes.UZS
import com.bano.cbrates.retrofit.valutes.VND
import com.bano.cbrates.retrofit.valutes.XDR
import com.bano.cbrates.retrofit.valutes.ZAR

data class Valute(
    val AED: AED,
    val AMD: AMD,
    val AUD: AUD,
    val AZN: AZN,
    val BGN: BGN,
    val BRL: BRL,
    val BYN: BYN,
    val CAD: CAD,
    val CHF: CHF,
    val CNY: CNY,
    val CZK: CZK,
    val DKK: DKK,
    val EGP: EGP,
    val EUR: EUR,
    val GBP: GBP,
    val GEL: GEL,
    val HKD: HKD,
    val HUF: HUF,
    val IDR: IDR,
    val INR: INR,
    val JPY: JPY,
    val KGS: KGS,
    val KRW: KRW,
    val KZT: KZT,
    val MDL: MDL,
    val NOK: NOK,
    val NZD: NZD,
    val PLN: PLN,
    val QAR: QAR,
    val RON: RON,
    val RSD: RSD,
    val SEK: SEK,
    val SGD: SGD,
    val THB: THB,
    val TJS: TJS,
    val TMT: TMT,
    val TRY: TRY,
    val UAH: UAH,
    val USD: USD,
    val UZS: UZS,
    val VND: VND,
    val XDR: XDR,
    val ZAR: ZAR
)